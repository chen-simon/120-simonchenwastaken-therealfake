const Header = (props) => {

    return (
        <div id="header">
            <div>Oishii Cart</div>
            <button>Sign In With Google</button>
        </div>
        
    );
}

export default Header;